import type { ThemeModes } from '@atlaskit/theme/types';
/**
 * Returns the background color depending on the passed through mode.
 * @param mode
 */
export declare const getBackground: (mode?: ThemeModes) => "#1B2638" | "#FFFFFF";
