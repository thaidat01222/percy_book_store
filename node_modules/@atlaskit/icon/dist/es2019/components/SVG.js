/** @jsx jsx */
import { memo } from 'react';
import { css, jsx } from '@emotion/core';
import GlobalTheme from '@atlaskit/theme/components';
import { getBackground } from './utils';
import { getSVGStyles, getSizeStyles } from './styles';
const PrimitiveSVGIcon = /*#__PURE__*/memo(function PrimitiveSVGIcon({
  size = 'medium',
  label,
  primaryColor,
  secondaryColor,
  testId,
  children
}) {
  return jsx(GlobalTheme.Consumer, null, ({
    mode
  }) => jsx("svg", {
    viewBox: "0 0 24 24",
    css: [getSVGStyles({
      primaryColor,
      secondaryColor: secondaryColor || getBackground(mode)
    }), css`
              ${getSizeStyles({
      size
    })}
            `],
    "data-testid": testId,
    "aria-label": label,
    role: "presentation",
    children: children
  }));
});
export default PrimitiveSVGIcon;